apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "traefik-canary.fullname" . }}-test-connection"
  labels:
{{ include "traefik-canary.labels" . | indent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args:  ['{{ include "traefik-canary.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
